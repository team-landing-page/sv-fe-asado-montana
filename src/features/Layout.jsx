import { Outlet } from "react-router-dom";

//Implement dashboard layout here
//For the moment is just created the layout to complete routing
const Layout = () => {
  return (
    <>
        <Outlet />
    </>
  )
}

export default Layout